# Expected Open Weights

Place the upstream MediumVC checkpoints here with the filenames expected by
`Any2Any/infer/infer_config.yaml`:

- `checkpoint-3000.pt`
- `checkpoint-3900.pt`

The official MediumVC README describes downloading the pretrained model and then
editing `Any2Any/infer/infer_config.yaml` if your layout differs.

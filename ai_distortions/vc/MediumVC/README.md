# MediumVC Notes

This package keeps only the inference-time code path used by `generate.py`.

External weights are not bundled. Follow the official MediumVC repository
instructions and place the downloaded assets at the paths referenced by
`Any2Any/infer/infer_config.yaml`.

Official repo: https://github.com/BrightGu/MediumVC

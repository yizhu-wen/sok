# Expected Open Weights

This directory already includes the vocoder config file `config.json`.

Download the matching upstream HiFi-GAN checkpoint and place it here as:

- `g_02500000`

The path is referenced directly by `Any2Any/infer/infer_config.yaml`.

# Shared venv for the Timbre and AWARE workers (modern torch stack).
# torch/torchaudio are installed separately from the PyTorch CPU wheel index:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.7.0 torchaudio==2.7.0
# Pins follow requirements/aware.txt and requirements/timbre.txt where the two
# manifests agree (librosa 0.9.2 in both); numba/llvmlite are lifted to
# versions that support this image's Python.

numpy==1.26.4
scipy==1.11.4
librosa==0.9.2
numba==0.59.0
llvmlite==0.42.0
resampy==0.4.2
soundfile==0.12.1
pyyaml==6.0.1
pydantic==2.5.0
tqdm==4.66.1

# AWARE (silence check)
webrtcvad==2.0.10

# Timbre model imports (distortion layer + vocoder deps)
julius==0.2.7
kornia==0.7.3
audiomentations==0.28.0
